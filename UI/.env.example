# URL da Web API NestJS (exposta no bundle do cliente — usada pelo Axios no browser)
NEXT_PUBLIC_API_URL=http://localhost:3000
# URL privada usada somente nas API Routes (não exposta no bundle do cliente)
API_URL=http://localhost:3000
